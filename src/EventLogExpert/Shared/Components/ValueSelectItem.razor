@typeparam T

<div id="@ItemId" selected="@IsSelected" @onmousedown="SelectItem">
    @if (ChildContent is not null)
    {
        @ChildContent
    }
    else
    {
        @DisplayString
    }
</div>
