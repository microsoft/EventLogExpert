@typeparam T

<div class="@CssClass" selected="@_isSelected" @onmousedown="SelectItem">
    @if (ChildContent is not null)
    {
        @ChildContent
    }
    else
    {
        @DisplayString
    }
</div>
