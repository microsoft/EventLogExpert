<FilterEditorCore CachedOptions="@null"
    Id="@Id"
    OnExclusionChanged="OnExclusionChanged"
    OnPendingDiscard="OnPendingDiscard"
    OnPendingSave="OnPendingSave"
    OnRemove="OnRemove"
    OnSave="OnSave"
    OnToggleEnabled="OnToggleEnabled"
    PendingDraft="PendingDraft"
    @ref="_coreRef"
    Value="Value" />
