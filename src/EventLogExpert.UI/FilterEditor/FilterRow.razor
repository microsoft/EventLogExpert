@inherits FilterRowBase<SavedFilter?>

<FilterEditorCore CachedOptions="@CachedOptions"
    Id="@Id"
    OnCancel="OnCancelFromCore"
    OnEdit="OnEditFromCore"
    OnExclusionChanged="OnExclusionChangedFromCore"
    OnPendingDiscard="OnPendingDiscardFromCore"
    OnPendingSave="OnPendingSaveFromCore"
    OnRemove="OnRemoveFromCore"
    OnSave="OnSaveFromCore"
    OnToggleEnabled="OnToggleEnabledFromCore"
    PendingDraft="PendingDraft"
    @ref="_coreRef"
    Value="Value" />
