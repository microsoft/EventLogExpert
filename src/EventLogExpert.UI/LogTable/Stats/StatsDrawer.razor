@inherits AppStateComponentBase

<div class="stats-drawer@(IsOpen ? " stats-drawer-open" : "")" id="stats-drawer">
    <div aria-hidden="true" class="stats-drawer-resizer" data-tooltip="@Localizer["Stats_Drawer_ResizeTitle"]"></div>
    @if (IsOpen)
    {
        <StatsPane />
    }
</div>
