@inherits AppStateComponentBase

@if (OpenLogsSource.HasOpenLogs)
{
    <FilterPane />

    <LensBreadcrumb />

    @if (HistogramVisibilitySource.IsVisible)
    {
        <HistogramPane />
    }

    <LogTablePane />

    <DetailsPane />
}
else
{
    <EmptyStateDashboard />
}
